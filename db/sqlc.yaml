version: "2"
sql:
  - engine: "postgresql"
    schema: "schema.sql"
    queries: "repositories/assignments/*.sql"
    gen:
      go:
        package: "assignments"
        out: "repositories/assignments"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_interface: true
        json_tags_case_style: "camel"

  - engine: "postgresql"
    schema: "schema.sql"
    queries: "repositories/cartAssignments/*.sql"
    gen:
      go:
        package: "cart_assignments"
        out: "repositories/cartAssignments"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_interface: true
        json_tags_case_style: "camel"

  - engine: "postgresql"
    schema: "schema.sql"
    queries: "repositories/cartShifts/*.sql"
    gen:
      go:
        package: "cart_shifts"
        out: "repositories/cartShifts"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_interface: true
        json_tags_case_style: "camel"

  - engine: "postgresql"
    schema: "schema.sql"
    queries: "repositories/carts/*.sql"
    gen:
      go:
        package: "carts"
        out: "repositories/carts"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_interface: true
        json_tags_case_style: "camel"

  - engine: "postgresql"
    schema: "schema.sql"
    queries: "repositories/cleaning/*.sql"
    gen:
      go:
        package: "cleaning"
        out: "repositories/cleaning"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_interface: true
        json_tags_case_style: "camel"

  - engine: "postgresql"
    schema: "schema.sql"
    queries: "repositories/meetings/*.sql"
    gen:
      go:
        package: "meetings"
        out: "repositories/meetings"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_interface: true
        json_tags_case_style: "camel"

  - engine: "postgresql"
    schema: "schema.sql"
    queries: "repositories/houseVisits/*.sql"
    gen:
      go:
        package: "house_visits"
        out: "repositories/houseVisits"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_interface: true
        json_tags_case_style: "camel"

  - engine: "postgresql"
    schema: "schema.sql"
    queries: "repositories/rooms/*.sql"
    gen:
      go:
        package: "rooms"
        out: "repositories/rooms"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_interface: true
        json_tags_case_style: "camel"

  - engine: "postgresql"
    schema: "schema.sql"
    queries: "repositories/sessions/*.sql"
    gen:
      go:
        package: "sessions"
        out: "repositories/sessions"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_interface: true
        json_tags_case_style: "camel"

  - engine: "postgresql"
    schema: "schema.sql"
    queries: "repositories/subsessions/*.sql"
    gen:
      go:
        package: "subsessions"
        out: "repositories/subsessions"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_interface: true
        json_tags_case_style: "camel"

  - engine: "postgresql"
    schema: "schema.sql"
    queries: "repositories/territories/*.sql"
    gen:
      go:
        package: "territories"
        out: "repositories/territories"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_interface: true
        json_tags_case_style: "camel"

  - engine: "postgresql"
    schema: "schema.sql"
    queries: "repositories/users/*.sql"
    gen:
      go:
        package: "users"
        out: "repositories/users"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_interface: true
        json_tags_case_style: "camel"

overrides:
  go:
    overrides:
      - db_type: "weekday_enum"
        go_type: "string"
      - db_type: "meeting_type_enum"
        go_type: "string"
      - db_type: "assignment_type_enum"
        go_type: "string"
      - db_type: "cleaning_type_enum"
        go_type: "string"
      - db_type: "visit_type_enum"
        go_type: "string"
      - db_type: "visit_category_enum"
        go_type: "string"
